# Copy to `.env` for local `npm run dev` / `npm run build`.
# On Netlify: Site settings → Environment variables → add PITCH_PASSWORD.

# Shared password that unlocks https://outorbit.games/pitch
# Only a SHA-256 hash of this value is embedded in the static page.
PITCH_PASSWORD=change-me
